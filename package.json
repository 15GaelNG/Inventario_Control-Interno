{
  "name": "inventario-ciudadmaderas",
  "private": true,
  "version": "0.1.0",
  "description": "Sistema de Control Interno (vehículos, telefonía, accesorios, caja chica) — Google Apps Script",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "login": "clasp login",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "open": "clasp open",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.0"
  }
}
